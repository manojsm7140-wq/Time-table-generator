# Production (MySQL)
Django>=4.2,<5.0
pandas>=2.0
openpyxl>=3.1
xlrd>=2.0.1
mysqlclient>=2.2.0
Pillow>=10.0
reportlab>=4.0

# Dev only (SQLite, no mysqlclient needed):
# pip install -r requirements_dev.txt
